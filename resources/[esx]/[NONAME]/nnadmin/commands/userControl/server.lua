-- local ESX = nil

-- TriggerEvent('esx:getShILovePizzaaredObjILovePizzaect', function(obj) 
-- 	ESX = obj 
-- end)

-- TriggerEvent('es:addGroupCommand', 'setusropt', 'superadmin', function(source, args, user)
		
-- 	local target = args[1]
-- 	local type = args[2]
-- 	local newSetting = args
--     table.remove(newSetting, 1)
--     table.remove(newSetting, 1)
--     newSetting = "" .. table.concat(newSetting, " ")

-- 	local xPlayer = ESX.GetPlayerFromId(target)
	
-- end)